# USB group ID for Stream Deck access (find with: getent group | grep -E 'usb|plugdev')
COMPANION_USB_GID=983

# Optional: Cloudflare Tunnel token for remote access
# Get from: https://one.dash.cloudflare.com/ -> Zero Trust -> Networks -> Tunnels
CLOUDFLARE_TUNNEL_TOKEN=
